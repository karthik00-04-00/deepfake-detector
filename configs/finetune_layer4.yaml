project:
  name: deepfake-detector

data:
  data_dir: data/processed/ffpp
  image_size: 128
  num_workers: 4

model:
  num_classes: 2

training:
  seed: 42
  batch_size: 32
  epochs: 5
  learning_rate: 0.0001
  finetune_layer4: true

logging:
  save_dir: outputs
